A boilerplate codebase for React Native with Redux integrated.

Initially created from [Create React Native App](https://github.com/react-community/create-react-native-app).

Run

### `npm install`

and then

### `npm start`

to start the sample app!
